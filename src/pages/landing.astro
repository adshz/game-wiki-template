---
/**
 * Landing page (/landing, English) — introduces the AnvilWiki open-source
 * project. NOT the demo game site (that's at /). 中文版在 /zh/landing。
 *
 * Sections (top to bottom):
 *   1. Hero (title + CTAs + install command)
 *   2. SocialProofBar (Lighthouse 4×100)
 *   3. FeatureGrid (6 cards)
 *   4. CompareTable (vs Fandom / Starlight / DIY)
 *   5. Showcase (demo screenshots)
 *   6. DocsEntry (3 doc cards)
 *   7. FinalCta
 */
import LandingLayout from '~/components/landing/LandingLayout.astro';
import LandingHero from '~/components/landing/LandingHero.astro';
import SocialProofBar from '~/components/landing/SocialProofBar.astro';
import FeatureGrid from '~/components/landing/FeatureGrid.astro';
import CompareTable from '~/components/landing/CompareTable.astro';
import Showcase from '~/components/landing/Showcase.astro';
import DocsEntry from '~/components/landing/DocsEntry.astro';
import FinalCta from '~/components/landing/FinalCta.astro';
import Community from '~/components/landing/Community.astro';
import { landingContent } from '~/config/landing';
import { siteUrl } from '~/config/site';

const t = landingContent.en;
const alternates = [
  { hreflang: 'en', href: `${siteUrl}/landing` },
  { hreflang: 'zh', href: `${siteUrl}/zh/landing` },
];
---

<LandingLayout locale="en" alternates={alternates}>
  <LandingHero t={t} />
  <SocialProofBar t={t} />
  <FeatureGrid t={t} />
  <CompareTable t={t} />
  <Showcase t={t} />
  <DocsEntry t={t} />
  <Community t={t} />
  <FinalCta t={t} />
</LandingLayout>
